Geoconv

Geoconv is a mobile application with GPS tools, Google Maps and Conversion of Coordinates

The application stores the points on database. These records can be modified and exported to CSV or KML format.

The main goal of this application is provide a easy way to stores locations marked with GPS or Google Maps, convert coordinates to UTM or Latitude/Longitude, order the records and export to vector layer or comma separated file.

